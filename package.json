{
  "name": "bkstg-devops",
  "version": "1.0.0",
  "description": "Dev-ops flow for Acuity backstage",
  "scripts": {
    "gen": "rimraf scripts && doable README.md scripts && doable docs/pre-reqs.md scripts && node bin/cp.js && node scripts/bin/chmod.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "acuity",
    "backstage",
    "app",
    "azure",
    "dev-ops"
  ],
  "author": "Shishir Ramam",
  "license": "ISC",
  "dependencies": {
    "doable": "github:acuity-sr/doable",
    "doctoc": "^2.0.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
